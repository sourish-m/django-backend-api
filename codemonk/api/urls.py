# from django.urls import path
# from .views import ParagraphSplitView, WordSearchView, RegisterView

# urlpatterns = [

#   path('split/', ParagraphSplitView.as_view(), name='split'),

#   path('search/', WordSearchView.as_view(), name='search'),

#   path('register/', RegisterView.as_view(), name='register')

# ]
